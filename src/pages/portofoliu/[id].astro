---
// Moved under /electrician/portofoliu/[id] — kept as a redirect for old
// links/bookmarks/SEO.
export const prerender = false;
const { id } = Astro.params;
return Astro.redirect(`/electrician/portofoliu/${id}`, 301);
---
