---
// Moved under /electrician/portofoliu (the electrician mini-site now owns
// its own portfolio page) — kept as a redirect for old links/bookmarks/SEO.
export const prerender = false;
return Astro.redirect("/electrician/portofoliu", 301);
---
