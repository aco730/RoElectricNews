---
// The wizard now lives inside /electrician/calculatoare (see
// src/components/OfertaWizard.astro, mounted from
// src/pages/electrician/[...slug].astro) — kept as a redirect for old
// links/bookmarks/SEO.
export const prerender = false;
return Astro.redirect("/electrician/calculatoare", 301);
---
